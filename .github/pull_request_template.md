## Summary


## Verification

- [ ] `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests`
- [ ] `gitleaks detect --config config/gitleaks.toml --redact --no-banner`

## Security Notes

Mention any changes to git-crypt, prompt injection, MCP tools, hooks, or CI.
